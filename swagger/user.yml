resourcePath: /Business
description: Business API's
apis:

- path: /api/user
  operations:

    - httpMethod: POST
      summary: Business_sign_up
      notes:  Manadatory Params  (For app - email)
      nickname: add_admin
      consumes:
        - application/json
      parameters:

      - name: body
        dataType: body
        defaultValue: '{
                          "email":"udit.kumawat@gmail.com"
                        }'
        paramType: body
        required: false
        description:
      responses:
        200:
         description: An array of keys